These are small wrappers around Flatpak apps.

They allow me to run Flatpak apps from the command line with shorter and more
obvious commands (e.g., `firefox`) rather than the longer, unambiguous commands
that are ordinarily required (e.g. `flatpak run org.mozilla.firefox`, or
`org.mozilla.firefox` with some configuration).
