# Use the yellow color from the one_dark theme for source files.
#
# The one_dark theme prints source files in the same color as directories, which
# I find very confusing. This overrides the color for source files to the same
# yellow that the one_dark theme uses elsewhere, like for dates.
export EZA_COLORS="sc=38;2;229;192;123"
