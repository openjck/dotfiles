-- This plugin automatically saves folds when a file is closed and restores them
-- when the file is opened again. It saves and restores some other stuff, too,
-- like cursor position, as well as anything else specified in viewoptions, but
-- I don't care about those, and that's not why I use it.
MiniDeps.add('vim-scripts/restore_view.vim')
