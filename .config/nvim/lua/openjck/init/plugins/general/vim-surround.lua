-- mini offers a similar plugin (mini.surround), but it shadows the "s" key in
-- normal mode, which I use often and which I have strong muscle memory for.
--
-- The keybindings in tpope/vim-surround are a bit more unusual and a bit more
-- difficult to remember, but they don't shadow "s".
MiniDeps.add('tpope/vim-surround')
